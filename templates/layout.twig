<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}TicketFlow{% endblock %}</title>
    <link rel="stylesheet" href="/styles/index.css">
    <link rel="stylesheet" href="/styles/theme.css">
    <link rel="stylesheet" href="/styles/App.css">
    <link rel="stylesheet" href="/styles/dashboard.css">
    <link rel="stylesheet" href="/styles/tickets.css">
</head>
<body>
    <div class="app">
        {% include 'components/header.twig' %}
        
        <main>
            {% if flash %}
                <div class="toast-container" style="position: fixed; top: var(--space-lg); right: var(--space-lg); z-index: 9999; display: flex; flex-direction: column; gap: var(--space-sm);">
                    {% for message in flash %}
                        <div class="toast toast-{{ message.type }}" style="padding: var(--space-md) var(--space-lg); border-radius: var(--radius-md); background: var(--color-surface); box-shadow: var(--shadow-lg); border-left: 4px solid {{ message.type == 'success' ? 'var(--leaf-olive)' : 'var(--leaf-rust)' }}; transition: opacity 0.3s;">
                            {{ message.message }}
                        </div>
                    {% endfor %}
                </div>
            {% endif %}
            
            {% block content %}{% endblock %}
        </main>
    </div>
    
    {# Using new app.js with all features #}
    <script src="/js/app.js"></script>
    {% block scripts %}{% endblock %}
</body>
</html>
